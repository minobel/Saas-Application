name: 1 - Hello World GitHub actions

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Hello there
        run: echo "Hello! Runner-e ekhon amar code access ache."

      - name: Show files
        run: ls -R